import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import CodeBlock from "@theme/CodeBlock";

import JsPeekAtVariables from "!!raw-loader!./js/peekAtVariables.js";
import PythonPeekAtVariant from "!!raw-loader!./python/peekAtVariant.py";
import PythonPeekAtVariables from "!!raw-loader!./python/peekAtVariables.py";
import ReactPeekAtVariables from "!!raw-loader!./react/peekAtVariables.js";
import SwiftPeekAtVariants from "!!raw-loader!./swift/peekAtVariants.swift";
import SwiftPeekAtVariables from "!!raw-loader!./swift/peekAtVariables.swift";
import VuePeekAtVariables from "!!raw-loader!./vue/peekAtVariables.js";
import JavaPeekAtVariants from "!!raw-loader!./java/peekAtVariants.java";
import JavaPeekAtVariables from "!!raw-loader!./java/peekAtVariables.java";
import GoPeekAtVariants from "!!raw-loader!./go/peekAtVariants.go";
import GoPeekAtVariables from "!!raw-loader!./go/peekAtVariables.go";
import DotNetPeekAtVariants from "!!raw-loader!./dotnet/peekAtVariants.cs";
import DotNetPeekAtVariables from "!!raw-loader!./dotnet/peekAtVariables.cs";
import PHPPeekAtVariant from "!!raw-loader!./php/peekAtVariant.php";
import PHPPeekAtVariable from "!!raw-loader!./php/peekAtVariable.php";
import RubyPeekAtVariant from "!!raw-loader!./ruby/peekAtVariants.rb";
import RubyPeekAtVariable from "!!raw-loader!./ruby/peekAtVariables.rb";
import FlutterPeekAtVariant from "!!raw-loader!./flutter/peekAtVariants.dart";
import FlutterPeekAtVariable from "!!raw-loader!./flutter/peekAtVariables.dart";

<Tabs groupId="language">

<TabItem value="js" label="Javascript">

Although generally not recommended, it is sometimes necessary to peek at a
treatment without triggering an exposure. The ABsmartly SDK provides a
`peek()` method for that.

<CodeBlock language="js">{JsPeekAtVariables}</CodeBlock>

</TabItem>

<TabItem value="python" label="Python3">

Although generally not recommended, it is sometimes necessary to peek at a
treatment without triggering an exposure. The ABsmartly SDK provides a
`peek_treatment()` method for that.

<CodeBlock language="py">{PythonPeekAtVariant}</CodeBlock>

#### Peeking at variables

<CodeBlock language="py">{PythonPeekAtVariables}</CodeBlock>

</TabItem>

<TabItem value="react" label="React">

Although generally not recommended, it is sometimes necessary to peek at a
treatment without triggering an exposure. The ABsmartly SDK provides a
`peek()` method for that.

<CodeBlock language="jsx">{ReactPeekAtVariables}</CodeBlock>

</TabItem>

<TabItem value="swift" label="Swift">

Although generally not recommended, it is sometimes necessary to peek at a
treatment without triggering an exposure. The ABsmartly SDK provides a
`peekTreatment()` method for that.

<CodeBlock language="swift">{SwiftPeekAtVariants}</CodeBlock>

##### Peeking at Variables

<CodeBlock language="swift">{SwiftPeekAtVariables}</CodeBlock>

</TabItem>

<TabItem value="vue" label="Vue2">

Although generally not recommended, it is sometimes necessary to peek at a
treatment without triggering an exposure. The ABsmartly SDK provides a
`peek()` method for that.

<CodeBlock language="js">{VuePeekAtVariables}</CodeBlock>

</TabItem>

<TabItem value="vue3" label="Vue3">

Although generally not recommended, it is sometimes necessary to peek at a
treatment without triggering an exposure. The ABsmartly SDK provides a
`peek()` method for that.

<CodeBlock language="js">{VuePeekAtVariables}</CodeBlock>

</TabItem>

<TabItem value="java" label="Java">

Although generally not recommended, it is sometimes necessary to peek at a
treatment or variable without triggering an exposure. The ABsmartly SDK
provides a `peekTreatment()` method for that.

<CodeBlock language="java">{JavaPeekAtVariants}</CodeBlock>

##### Peeking at variables

<CodeBlock language="java">{JavaPeekAtVariables}</CodeBlock>

</TabItem>

<TabItem value="go" label="Go">

Although generally not recommended, it is sometimes necessary to peek at a
treatment or variable without triggering an exposure. The ABsmartly SDK
provides a `PeekTreatment()` method for that.

<CodeBlock language="go">{GoPeekAtVariants}</CodeBlock>

##### Peeking at variables

<CodeBlock language="go">{GoPeekAtVariables}</CodeBlock>

</TabItem>

<TabItem value="dotnet" label=".NET">

Although generally not recommended, it is sometimes necessary to peek at
a treatment or variable without triggering an exposure. The ABsmartly
SDK provides a `PeekTreatment()` method for that.

<CodeBlock language="csharp">{DotNetPeekAtVariants}</CodeBlock>

##### Peeking at variables

<CodeBlock language="csharp">{DotNetPeekAtVariables}</CodeBlock>

</TabItem>

<TabItem value="php" label="PHP">

Although generally not recommended, it is sometimes necessary to peek at a
treatment or variable without triggering an exposure. The ABsmartly SDK
provides a `Context->peekTreatment()` method for that.

<CodeBlock language="php">{PHPPeekAtVariant}</CodeBlock>

#### Peeking at variables

<CodeBlock language="php">{PHPPeekAtVariable}</CodeBlock>

</TabItem>

<TabItem value="ruby" label="Ruby">

Although generally not recommended, it is sometimes necessary to peek at a
treatment or variable without triggering an exposure. The ABsmartly SDK
provides a `peek_treatment()` method for that.

<CodeBlock language="ruby">{RubyPeekAtVariant}</CodeBlock>

#### Peeking at variables

<CodeBlock language="ruby">{RubyPeekAtVariable}</CodeBlock>

</TabItem>

<TabItem value="flutter" label="Flutter/Dart">

Although generally not recommended, it is sometimes necessary to peek at a
treatment or variable without triggering an exposure. The ABsmartly SDK
provides a `peekTreatment()` method for that.

<CodeBlock language="dart">{FlutterPeekAtVariant}</CodeBlock>

#### Peeking at variables

<CodeBlock language="dart">{FlutterPeekAtVariable}</CodeBlock>

</TabItem>

</Tabs>
