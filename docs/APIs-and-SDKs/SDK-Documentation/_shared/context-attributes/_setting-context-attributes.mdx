import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import CodeBlock from "@theme/CodeBlock";

import JsSetAttributes from "!!raw-loader!./js/setAttributes.js";

import PythonSetAttributes from "!!raw-loader!./python/setAttributes.py";

import ReactSetAttributes from "!!raw-loader!./react/setAttributes.js";
import ReactSetAttributesAsProps from "!!raw-loader!./react/setAttributesAsProps.tsx";

import SwiftSetAttributes from "!!raw-loader!./swift/setAttributes.swift";

import VueSetAttributesScript from "!!raw-loader!./vue/setAttributes.js";
import VueSetAttributesTemplate from "!!raw-loader!./vue/SetAttributes.vue";

import JavaSetAttributes from "!!raw-loader!./java/setAttributes.java";

import GoSetAttributes from "!!raw-loader!./go/setAttributes.go";

import DotNetSetAttributes from "!!raw-loader!./dotnet/setAttributes.cs";

import PHPSetAttributes from "!!raw-loader!./php/setAttributes.php";

import RubySetAttributes from "!!raw-loader!./ruby/setAttributes.rb";

import FlutterSetAttributes from "!!raw-loader!./flutter/setAttributes.dart";

Set attributes using the `attribute()` or `attributes()` methods, before or
after the context is ready.

A particularly useful attribute is `user_agent`. When you pass it to the SDK,
ABsmartly automatically parses it into browser, OS, and device attributes that
you can use for targeting in the Web Console. See
[Segments in Dashboard Settings](/docs/web-console-docs/configuration/settings#segments)
for details.

<Tabs groupId="language">

<TabItem value="js" label="Javascript">

<CodeBlock language="js">{JsSetAttributes}</CodeBlock>

</TabItem>

<TabItem value="python" label="Python3">

<CodeBlock language="py">{PythonSetAttributes}</CodeBlock>

</TabItem>

<TabItem value="react" label="React">

The context can be used directly to set attributes, like so:

<CodeBlock language="tsx">{ReactSetAttributes}</CodeBlock>

Or you can pass them as a prop to the `Treatment` or `TreatmentFunction` components.

<CodeBlock language="tsx">{ReactSetAttributesAsProps}</CodeBlock>

:::note
The `attribute()` and `attributes()` methods can be called inside or outside
of a useEffect.
:::

</TabItem>

<TabItem value="swift" label="Swift">

<CodeBlock language="swift">{SwiftSetAttributes}</CodeBlock>

</TabItem>

<TabItem value="vue" label="Vue2">

Attributes can be set in script.

<CodeBlock language="js">{VueSetAttributesScript}</CodeBlock>

Or directly in templates with the `:attributes` property of the `Treatment`
component.

<CodeBlock language="html">{VueSetAttributesTemplate}</CodeBlock>

</TabItem>

<TabItem value="vue3" label="Vue3">

Attributes can be set in script.

<CodeBlock language="js">{VueSetAttributesScript}</CodeBlock>

Or directly in templates with the `:attributes` property of the `Treatment`
component.

<CodeBlock language="html">{VueSetAttributesTemplate}</CodeBlock>

</TabItem>

<TabItem value="java" label="Java">

<CodeBlock language="java">{JavaSetAttributes}</CodeBlock>

</TabItem>

<TabItem value="go" label="Go">

<CodeBlock language="go">{GoSetAttributes}</CodeBlock>

</TabItem>

<TabItem value="dotnet" label=".NET">

<CodeBlock language="csharp">{DotNetSetAttributes}</CodeBlock>

</TabItem>

<TabItem value="php" label="PHP">

<CodeBlock language="php">{PHPSetAttributes}</CodeBlock>

</TabItem>

<TabItem value="ruby" label="Ruby">

<CodeBlock language="ruby">{RubySetAttributes}</CodeBlock>

</TabItem>

<TabItem value="flutter" label="Flutter/Dart">

<CodeBlock language="dart">{FlutterSetAttributes}</CodeBlock>

</TabItem>

</Tabs>

:::caution
You should create audiences with the same attributes as those in the Web Console.
By default it will not enforce these conditions, but if for some reason the
`treatment()` method is called for a user that doesn’t meet the correct criteria, the
Web Console will warn you about it.
:::
